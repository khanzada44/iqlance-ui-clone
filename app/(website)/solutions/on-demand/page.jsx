import Ondemand from "../../../components/pages/on-demand/page";
export const metadata = {
  title: "On-Demand App Development Company for Apps That Connect | DevApp Grid",
  description: "Build an on-demand app that goes beyond the idea. DevApp Grid handles booking, dispatch, payments, tracking, and scalable user experiences.",
};

export default function Devapp() {
  return <Ondemand />;
}
